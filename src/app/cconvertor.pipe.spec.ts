import { CconvertorPipe } from './cconvertor.pipe';

describe('CconvertorPipe', () => {
  it('create an instance', () => {
    const pipe = new CconvertorPipe();
    expect(pipe).toBeTruthy();
  });
});
